#Riley Miller, CSCI471 Project

To compile program type make.

To run verbose mode pass in the -v flag

To quit program and kill server type "QUIT" from the client.

To stop client connection but keep server socket running, type "CLOSE" from the client.

To connect to the server use `telnet isengard.mines.edu <port number>` to connect.


